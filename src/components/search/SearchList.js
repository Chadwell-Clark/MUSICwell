export const SearchList = ({searchInputText}) => {
    //   *Take text >>  query to table that returns array of objects >>
    console.log("searchInputText", searchInputText);    
    return (
        <>
        <div>{searchInputText}</div>
        </>
    )
}